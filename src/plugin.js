/**
 * WordPress dependencies.
 */
import { render } from "@wordpress/element";

/**
 * Internal dependencies.
 */
import Optigration from './components/optigration';

/**
 * Render optigration plugin.
 */
render( <Optigration />, document.getElementById( 'optigration' ) );
